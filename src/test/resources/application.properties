# Test configuration: never launch llama-server or read stdin during the build.
llama.manage-server=false
roleflow.terminal.enabled=false
roleflow.system-prompt=
# Keep the audit log out of the repo root during tests.
roleflow.audit.log-file=target/audit-test.log
